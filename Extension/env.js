const COGNITO_CONFIG = {
  userPoolId: add_your_userPool,
  domain: add_your_domain,
  appClientId: add_your_appClientId,
  region: add_your_region
};

const GOOGLE_CONFIG = {
  clientId: add_your_clientId
};

const MICROSOFT_CONFIG = {
    clientId: add_your_clientId
};

const API_CONFIG = {
    baseUrl: add_your_baseUrl,
    syncListEndpoint: add_your_syncListEndpoint
};
